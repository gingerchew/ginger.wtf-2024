<!DOCTYPE html>
<html lang="{{ meta.lang }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ meta.title }}</title>
    <link rel="me" href="https://queer.party/@gingerchew" />
	<link rel="preload" href="/assets/fonts/Fraunces[SOFT,WONK,opsz,wght].woff2" as="font" type="font/woff2" crossorigin="">
	<meta name="view-transition" content="same-origin" />
    {% include "meta.njk" %}

	<script>
        var { mode, theme } = localStorage;
	    if (mode) document.documentElement.dataset.mode = mode;
	    if (theme) document.documentElement.dataset.theme = theme;
	</script>
    <style>
		p {
			text-wrap: pretty;
		}
		:is(h1,h2,h3,.h1,.h2,.h3) {
			text-wrap: balance;
		}
	</style>
	<link rel="stylesheet" href="/assets/css/main.scss">
</head>
<body class="layout-{{layout}}">
    <a href="#main" class="skiplink">Skip to main content</a>

    <div class="layout" role="document">
        {% include "header.njk" %}
        <main id="main" class="main">
            {{ content | safe }}
        </main>
        {% include "footer.njk" %}
    </div>

    <script type="module" src="{{ '/assets/js/main.js' | url }}"></script>
    <script defer data-domain="ginger.wtf" src="https://plausible.io/js/script.js"></script>
</body>
</html>
